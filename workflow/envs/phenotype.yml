name: phenotype
channels:
  - conda-forge
dependencies:
  - scikit-image
  - scipy
  - joblib
  - cellpose # TODO: pin version if reproducbility is important (version may change segmentations)
  - pytables # need pytables defined in module env
  - microfilm
  - mahotas
