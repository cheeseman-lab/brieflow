name: sbs_process
channels:
  - conda-forge
dependencies:
  - python=3.11
  - scikit-image
  - scipy
  - joblib
  - cellpose # TODO: pin version if reproducbility is important (version may change segmentations)
  - pytables
  - seaborn
